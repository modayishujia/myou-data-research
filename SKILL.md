---
name: data-research
description: >
  全天候数据调研 skill。基于8维度调研框架（搜索热度/内容生态/评论区情绪/KOL画像/竞品对比/话题发酵/风险评估/预测研判），
  持续采集网络数据、社交媒体、评论区信息，增量更新本地 JSON 存储，追踪话题在时间维度的发酵与演变过程。
  首批数据返回后自动启动本地 Flask 看板，文字为主、ECharts 辅助可视化，绝不省略文字。
  看板页面自动轮询刷新（无需手动F5），始终显示最新数据和更新时间。
  面板核心目的：从社交网络和网络信息中发现问题，而非展示数据。
  支持4种场景预设：舆情监控、行业调研、产品发布、投资研究，每种场景有独立的目标对象和分析焦点。
  使用场景：用户说"调研XX"、"帮我看看XX的舆情"、"追踪XX事件变化"、"XX话题发酵过程"、
  "数据调研"、"舆情监控"、"信息采集"、"deep research"、"帮我收集XX的信息"。
  触发词：调研、追踪、舆情、发酵、演变、监控、采集、trending、research。
---

# 数据调研 Skill

全天候数据采集 → 增量存储 → 8维度分析 → 问题发现 → 看板自动刷新渲染。

## 核心原则

1. **问题发现导向**：面板目的是从社交网络和网络信息中发现问题，不是展示数据
2. **文字绝不省略**：看板以文字为主体，图表仅作辅助，所有数据必须有完整文字描述
3. **数据可读性优先**：每条数据必须有明确的语境，不能出现孤立数字或无意义的列表
4. **增量持续更新**：每次采集新数据后立即写入 JSON，不覆盖历史
5. **演变过程追踪**：关注话题随时间的变化，而非单点快照
6. **信号敏感捕捉**：从变化中识别情感转向、关键词涌现、叙事框架变化等敏感因素
7. **时间必须最新**：所有数据条目使用真实采集时间戳，看板显示最新更新时间并自动刷新
8. **每轮必须深入评论区**：每个数据源（小红书/抖音等）都必须采集评论区数据，并做总结性归纳
9. **采集方式标注**：每条数据必须标注 `collection_method`（kimi-webbridge / web_search / webfetch）
10. **全部中文标签**：看板中来源、情感、阶段、信号等标签全部使用中文显示
11. **空模块隐藏**：看板中无数据的模块自动隐藏，不显示空白区域

## 场景预设与分析链路

每个场景有完整的分析链路，从数据采集到最终输出，形成闭环。

### 场景自动匹配

从用户自然语言中识别场景关键词，匹配最接近的场景。默认「舆情监控」。

匹配规则（优先级从高到低）：
1. **投资研究**：股票/投资/估值/财报/业绩/基金/股价/市值/持仓/证券/上市公司
2. **产品发布**：发布/新品/上市/首发/预售/交付/提车/开售/发布会
3. **行业调研**：行业/赛道/市场/趋势/产业/政策/创新药/技术演进
4. **舆情监控**（默认）：舆情/口碑/评价/争议/品牌/事件

### 场景一：投资研究

**目标**：从社交网络和网络信息中发现上市公司的投资机会和风险

**分析链路**：
```
上市公司 → 大单品/核心业务 → 社交平台评论 → 意图分析 → 市场认知 → 投资判断 → 交易策略
```

| 阶段 | 采集目标 | 分析输出 | 面板模块 |
|------|----------|----------|----------|
| 1. 上市公司 | 公司基本面、最新公告、股价异动 | 公司概况卡片 | 核心发现 |
| 2. 大单品 | 核心产品/业务线的市场表现 | 产品矩阵分析 | 数据条目 |
| 3. 社交评论 | 小红书/抖音/雪球/东方财富评论区 | 评论区总结 | 评论区洞察 |
| 4. 意图分析 | 用户讨论中的投资意图（买入/观望/卖出/质疑） | 意图分布 | 核心发现 |
| 5. 市场认知 | 市场对公司的主流认知框架 | 叙事分析 | 核心发现 |
| 6. 投资判断 | 基于数据的投资机会/风险判断 | 信号与建议 | 信号面板 |
| 7. 交易策略 | 基于分析的策略建议 | 策略输出 | 核心发现 |

**关键词矩阵**：
- 第一层：公司名、股票代码（药明康德、603259）
- 第二层：核心产品/业务（TIDES、ADC、CRDMO）
- 第三层：竞品公司（恒瑞、百济神州、信达）
- 第四层：财务词（财报、业绩、营收、净利、增速）
- 第五层：市场词（估值、股价、机构评级、持仓）

### 场景二：产品舆情

**目标**：从社交网络和网络信息中发现产品的市场反馈和传播机会

**分析链路**：
```
产品 → 主内容+评论区 → 意图分析 → 市场认知 → 传播策略
```

| 阶段 | 采集目标 | 分析输出 | 面板模块 |
|------|----------|----------|----------|
| 1. 产品定位 | 产品核心卖点、目标用户、价格带 | 产品画像 | 核心发现 |
| 2. 主内容 | KOL评测、媒体报道、用户UGC | 内容生态分析 | 数据条目 |
| 3. 评论区 | 小红书/抖音/微博评论区 | 评论区总结 | 评论区洞察 |
| 4. 意图分析 | 用户讨论中的购买意图（必买/观望/放弃/对比） | 意图分布 | 核心发现 |
| 5. 市场认知 | 市场对产品的主流认知（好评点/槽点/盲区） | 认知地图 | 核心发现 |
| 6. 传播策略 | 基于分析的传播建议 | 策略输出 | 信号面板 |

**关键词矩阵**：
- 第一层：产品名、品牌名（小米YU7、小米汽车）
- 第二层：配置/功能词（续航、空间、智驾、内饰）
- 第三层：竞品词（Model Y、理想L7、问界M7）
- 第四层：决策词（价格、值不值、优缺点、口碑）
- 第五层：场景词（家庭、露营、通勤、长途）

### 场景三：行业调研

**目标**：从社交网络和网络信息中发现行业趋势和竞争格局变化

**分析链路**：
```
行业/赛道 → 市场数据 → 竞争格局 → 趋势判断 → 机会识别
```

| 阶段 | 采集目标 | 分析输出 | 面板模块 |
|------|----------|----------|----------|
| 1. 行业概况 | 行业规模、增速、政策环境 | 行业画像 | 核心发现 |
| 2. 市场数据 | 融资事件、IPO、并购、License-out | 事件时间线 | 演变时间线 |
| 3. 竞争格局 | 头部玩家、市场份额、差异化策略 | 竞争分析 | 数据条目 |
| 4. 趋势判断 | 技术趋势、资本趋势、政策趋势 | 趋势信号 | 信号面板 |
| 5. 机会识别 | 基于分析的投资/业务机会 | 机会输出 | 核心发现 |

**关键词矩阵**：
- 第一层：行业名、赛道名（创新药、CXO、ADC）
- 第二层：技术词（TIDES、双抗、基因治疗、AI制药）
- 第三层：公司词（药明康德、百济神州、恒瑞）
- 第四层：市场词（融资、IPO、License-out、出海）
- 第五层：政策词（BIOSECURE Act、集采、医保谈判）

### 场景四：舆情监控

**目标**：从社交网络和网络信息中发现品牌/事件的舆情风险和机会

**分析链路**：
```
品牌/事件 → 情感追踪 → 危机预警 → 叙事分析 → 应对建议
```

| 阶段 | 采集目标 | 分析输出 | 面板模块 |
|------|----------|----------|----------|
| 1. 品牌画像 | 品牌认知度、美誉度、核心叙事 | 品牌概况 | 核心发现 |
| 2. 情感追踪 | 各平台情感分布变化 | 情感趋势 | 评论区洞察 |
| 3. 危机预警 | 负面舆情萌芽、扩散风险 | 预警信号 | 信号面板 |
| 4. 叙事分析 | 品牌方/KOL/用户三方叙事占比 | 叙事格局 | 核心发现 |
| 5. 应对建议 | 基于分析的危机应对和传播建议 | 策略输出 | 信号面板 |

**关键词矩阵**：
- 第一层：品牌名、产品名
- 第二层：品牌关联词（创始人、代言人、母公司）
- 第三层：情感词（好评、差评、争议、翻车）
- 第四层：事件词（发布会、事故、召回、涨价）
- 第五层：竞品词（竞品品牌名、对比）

## 工作流程

### Phase 1: 话题解析与关键词矩阵

从用户自然语言描述中提取：
- **话题**：核心调研主题
- **关注维度**：情感倾向、关键人物、叙事框架、时间节点、利益相关方

#### 5 层关键词体系（必选，至少10组关键词）

| 层级 | 类型 | 示例（以小米SkyNomad为例） | 采集目的 |
|------|------|---------------------------|----------|
| 第一层 | 品牌词 + 产品词 | 小米SkyNomad、小米澎程 | 品牌/产品搜索热度 |
| 第二层 | 配置词 + 场景词 + 外观词 | 小米澎程N70价格、小米澎程帐篷露营、小米澎程白色实车 | 配置/场景/外观关注焦点 |
| 第三层 | 竞品词 | 小米澎程理想对比、小米澎程问界M9 | 竞品对比热度 |
| 第四层 | 决策词 + 评价词 | 小米澎程价格上市、小米澎程评价口碑 | 购买决策/口碑热度 |
| 第五层 | KOL词 | [KOL名称] 小米澎程 | KOL内容动态 |

输出格式：
```
话题: XXX事件舆情演变
关键词矩阵:
  第一层: [品牌词1, 产品词1, ...]
  第二层: [配置词1, 场景词1, 外观词1, ...]
  第三层: [竞品词1, 竞品词2, ...]
  第四层: [决策词1, 评价词1, ...]
  第五层: [KOL词1, ...]
关注维度: [搜索热度, 内容生态, 评论区情绪, KOL画像, 竞品对比, 话题发酵, 风险评估, 预测研判]
```

### Phase 2: 数据采集

使用 data_store.py 管理数据。数据目录：`~/.local/share/data-research/`

```bash
# 初始化话题
python3 scripts/data_store.py init "话题名" "关键词1,关键词2"
```

#### 采集流程（每轮必须完整执行 6 步）

**Step 1: Web 搜索（必选）**
- 使用 websearch 工具搜索每个关键词
- 搜索时间范围限定：首次为最近 7 天，后续为上次采集时间之后
- 提取：标题、摘要、URL、发布时间
- 若 websearch 不可用，用 webfetch 抓取百度搜索结果页

**Step 2: 小红书深度采集（必选）**

通过 kimi-webbridge 浏览小红书搜索结果和评论区：

```bash
# 1. 启动守护进程（如未运行）
~/.kimi-webbridge/bin/kimi-webbridge start

# 2. 打开小红书搜索（session 固定为 pengcheng-research）
curl -s -X POST http://127.0.0.1:10086/command \
  -H 'Content-Type: application/json' \
  -d '{"action":"navigate","args":{"url":"https://www.xiaohongshu.com/search_result?keyword=关键词&source=web_search_result_notes","newTab":true,"group_title":"数据调研-话题名"},"session":"pengcheng-research"}'

# 3. 等待页面加载后，用 evaluate 提取笔记列表
curl -s -X POST http://127.0.0.1:10086/command \
  -H 'Content-Type: application/json' \
  -d '{"action":"evaluate","args":{"code":"(() => { const items = document.querySelectorAll(\"section.note-item, [data-v-a264b0de], .feeds-page section\"); const results = []; items.forEach((item, i) => { if(i >= 20) return; const allText = item.innerText; results.push(allText.substring(0, 200)); }); return JSON.stringify({count: items.length, samples: results.slice(0, 15)}); })()"},"session":"pengcheng-research"}'
```

**Step 3: 抖音深度采集（必选）**

通过 kimi-webbridge 浏览抖音搜索结果和评论区：

```bash
# 打开抖音搜索
curl -s -X POST http://127.0.0.1:10086/command \
  -H 'Content-Type: application/json' \
  -d '{"action":"navigate","args":{"url":"https://www.douyin.com/search/关键词","newTab":true,"group_title":"数据调研-话题名"},"session":"pengcheng-research"}'

# 用 snapshot 获取搜索结果（抖音用 accessibility tree 效果好）
curl -s -X POST http://127.0.0.1:10086/command \
  -H 'Content-Type: application/json' \
  -d '{"action":"snapshot","args":{},"session":"pengcheng-research"}'
```

抖音搜索结果采集要点：
- `StaticText` 节点中的数字（如「475.6万」）是互动数据
- 搜索建议标签（如「n90价格」「系列定档」）反映用户关注热点
- 热榜标签和播放量反映话题整体热度

**Step 4: 评论区深度采集与总结归纳（必选，每个数据源都必须执行）**

对每个数据源（小红书、抖音等）的头部内容（互动量Top 10）进行评论区采样，并输出该平台的评论区总结：

```bash
# 评论区采集完成后，对每个平台输出总结：
# - 高频关键词 TOP 10
# - 情绪分布（正面/负面/中性/混合 各占比）
# - 典型正面评论（1-2条代表性原文）
# - 典型负面评论（1-2条代表性原文）
# - 价格预期（用户自发讨论的价格区间）
# - 核心关注点（空间/续航/价格/配置/品牌 等维度排序）
```

对头部内容（互动量Top 10）的评论区进行采样：

```bash
# 进入内容详情页
curl -s -X POST http://127.0.0.1:10086/command \
  -H 'Content-Type: application/json' \
  -d '{"action":"evaluate","args":{"code":"(() => { const items = document.querySelectorAll(\"section.note-item\"); const target = items[N]; if(target) { const a = target.querySelector(\"a\"); if(a) { a.click(); return \"clicked\"; } } return \"not found\"; })()"},"session":"pengcheng-research"}'

# 等待加载后提取评论
sleep 3
curl -s -X POST http://127.0.0.1:10086/command \
  -H 'Content-Type: application/json' \
  -d '{"action":"evaluate","args":{"code":"(() => { const comments = document.querySelectorAll(\"[class*=comment], .comment-item\"); const results = []; comments.forEach((c, i) => { if(i >= 15) return; const text = c.innerText?.trim(); if(text && text.length > 5 && text.length < 300) results.push(text); }); return JSON.stringify({commentCount: results.length, comments: results}); })()"},"session":"pengcheng-research"}'
```

**评论区情绪分类标准**（5类）：

| 情绪类型 | 关键词特征 | 示例表述 |
|----------|-----------|----------|
| 期待 | "等不及""必买""心动""冲" | "这配置太香了，等上市" |
| 焦虑 | "价格""什么时候""等太久了" | "到底多少钱，别挤牙膏了" |
| 观望 | "再看看""等等党""对比一下" | "等实车到店再决定" |
| 负面 | "割韭菜""不值""太贵""失望" | "这价格不如买XX" |
| 玩梗 | "哈哈哈""笑死""绝了" | "雷总又来卖房了" |

**Step 5: KOL 画像分析（按需）**

识别参与话题的头部/中腰部KOL，逐个分析：
- 账号定位和粉丝画像
- 对目标品牌的态度（强正面/正面/中性/负面）
- 内容偏好（评测/谍照/直播/拆解）
- 合作风险（过度绑定/立场摇摆/黑历史）

**KOL 分类框架**：

| 类型 | 特征 | 风险等级 |
|------|------|----------|
| 品牌专属型 | 内容100%围绕单一品牌 | 低 |
| 全品类测评型 | 覆盖所有品牌，以数据说话 | 低 |
| 平台级媒体 | 官方媒体账号，专业团队 | 低 |
| 辣评/观点型 | 以争议性内容为主 | 高 |
| 车友会/社区型 | 车主自发组织 | 低 |

**Step 6: 深度页面（按需）**
- 对高价值 URL 使用 webfetch 获取全文
- 提取文章中的关键论点和数据

#### 采集数据格式

每条数据写入 JSON，**timestamp 必须使用当前真实时间**：
```json
{
  "source": "xiaohongshu/douyin/web_search/comment_section",
  "content": "具体内容...",
  "url": "https://...",
  "timestamp": "当前ISO格式时间",
  "metadata": {
    "platform": "xiaohongshu/douyin/weibo/bilibili/web/...",
    "collection_method": "kimi-webbridge/web_search/webfetch/...",
    "author": "用户名",
    "engagement": "1.2万赞",
    "sentiment": "positive/negative/neutral/mixed",
    "sentiment_detail": "期待/焦虑/观望/负面/玩梗",
    "is_comment": true,
    "content_type": "官方/KOL/UGC/媒体",
    "kol_type": "品牌专属型/全品类测评型/..."
  }
}
```

#### 增量写入

采集后立即调用 data_store 的 add_entries 写入。脚本内置去重（基于内容哈希），不会重复写入。

### Phase 3: 8 维度分析与 KMI 监控

每轮采集后进行全维度分析：

#### 6 项 KMI（关键监测指标）

| KMI | 计算方式 | 健康阈值 | 预警阈值 |
|-----|----------|----------|----------|
| 正面情绪占比 | 正面评论数 / 总采样评论数 | >70% | <50% |
| 负面情绪占比 | 负面评论数 / 总采样评论数 | <15% | >25% |
| 价格焦虑指数 | 价格相关负面评论 / 总价格评论 | <30% | >50% |
| 疲劳指数 | "挤牙膏/什么时候"评论增长率 | 稳定或下降 | 连续3天增长 |
| KOL正面率 | 正面KOL数 / 已发声KOL总数 | >70% | <50% |
| 竞品对比负面率 | 竞品对比中"竞品更好"的占比 | <30% | >50% |

#### 分析维度

1. **搜索热度** — 搜索结果密度、头部视频互动量、相关搜索词、筛选标签
2. **内容生态** — 内容类型分布（官方/KOL/UGC/媒体）、形式分布、时间分布、互动量分布
3. **评论区情绪** — 情绪分布（5类）、高频关键词TOP20、极值表述、民间定价、疲劳指标
4. **KOL画像** — 品牌情绪分布、重点KOL画像、合作建议
5. **竞品对比** — 主要竞品识别、对比维度分析、用户倾向判断、越级对标分析
6. **话题发酵** — 萌芽期→扩散→爆发→衰减的完整路径
7. **风险评估** — 概率×影响×触发条件×预警指标×应对预案
8. **预测研判** — 热点发酵预测、风险演变预测、竞品动态预测、用户行为预测

#### 演变节点写入

```python
add_evolution_point(topic_id, {
    "phase": "emergence/growth/peak/decline/stable",
    "summary": "本轮变化总结",
    "key_changes": ["变化1", "变化2"],
    "sentiment_shift": "正面 → 负面",
    "new_keywords": ["新词1", "新词2"],
    "notable_events": ["事件1"],
    "kmi_snapshot": {
        "positive_ratio": 0.72,
        "negative_ratio": 0.12,
        "price_anxiety": 0.25,
        "fatigue_index": "stable",
        "kol_positive_rate": 0.68,
        "competitive_negative_rate": 0.28
    }
})
```

#### 信号写入

发现敏感信号时：
```python
add_signal(topic_id, {
    "type": "sentiment_shift/keyword_emergence/volume_spike/narrative_change/risk_trigger",
    "severity": "low/medium/high/critical",
    "description": "信号描述",
    "evidence": ["证据1", "证据2"],
    "recommended_action": "建议关注...",
    "risk_assessment": {
        "probability": "high/medium/low",
        "impact": "extremely_high/high/medium/low",
        "trigger_condition": "具体触发条件",
        "early_warning": "可量化监测指标",
        "response_plan": "响应时间+应对措施"
    }
})
```

### Phase 4: 看板渲染（自动刷新）

首批数据（≥5 条）采集完成后，启动本地看板：

```bash
# 安装依赖（首次）
pip install flask

# 启动看板（后台运行）
nohup python3 scripts/dashboard_server.py --port 8765 > /tmp/dashboard.log 2>&1 &
```

看板特性（暗色全屏监控大屏，问题发现导向）：
- **暗色主题**：深色背景 #0b0f1a + 高对比度文字，适合长时间监控
- **左右全屏、上下自由滚动**：内容区不限高，可随意滚动浏览
- **固定顶栏**：毛玻璃效果，显示 LIVE 状态、场景标签、话题名、上次更新、倒计时
- **顶部三栏关键总结**（非采集统计）：
  - **关键发现**：从评论区总结/信号中提取最重要的发现，展示具体洞察而非条数统计
  - **态势判断**：当前阶段 + 最新演变摘要，一句话概括当前状态
  - **风险预警**：严重/高风险信号数 + 最需关注的信号描述
- **核心发现**：场景自适应，顶部显示分析链路（如「分析链路：上市公司→大单品→评论→意图→市场认知→投资判断」），内容从实际数据自动生成
- **各平台评论区洞察**：每个平台独立卡片，含高频词/情绪分布/代表性评论/核心关注点
- **信号与建议**：从信号数据动态生成，按严重度排列，每个信号带建议
- **ECharts 图表区**：来源分布饼图 + 时间趋势柱状图 + 采集方式分布饼图，无数据的图表自动隐藏
- **演变时间线**：中文阶段标签 + 摘要 + 关键词标签
- **数据条目表格**：时间 | 来源(中文) | 采集方式 | 内容 | 情感(中文) | 互动量
- **自动轮询**：每 30 分钟自动刷新，倒计时 mm:ss 格式
- **空模块隐藏**：无数据的模块自动隐藏
- **全部中文标签**：来源/情感/阶段/信号/采集方式
- **场景自适应**：面板顶部显示当前场景标签（舆情监控/行业调研/产品发布/投资研究），核心发现的分析链路随场景变化

### Phase 5: 预测性研判输出

每轮分析输出 4 类预测：

| 预测类型 | 方法 | 输出格式 |
|----------|------|----------|
| 热点发酵预测 | 当前数据趋势 + 历史规律 | 预测结论 + 触发条件 + 跟踪指标 |
| 风险演变预测 | 风险当前状态 + 外部变量 | 当前状态 + 预测变化 + 管控动作 |
| 竞品动态预测 | 竞品历史动作 + 行业节奏 | 预测竞品可能动作 + 应对预案 |
| 用户行为预测 | 用户当前情绪 + 决策链路阶段 | 预测用户下一步行为 + 品牌应对策略 |

### Phase 6: 持续循环

采集 → 分析 → 更新看板 → 等待 → 再采集。循环策略：

| 阶段 | 观测频率 | 重点维度 |
|------|----------|----------|
| 发布前30天 | 每周1次 | 搜索热度、内容生态、KOL动态 |
| 发布前7天 | 每日1次 | 评论区情绪、竞品动态、疲劳指标 |
| 发布当天 | 每2小时1次 | 价格反馈、情绪走向、KOL反应 |
| 发布后7天 | 每日1次 | 口碑走向、对比内容、顶流动态 |
| 发布后30天 | 每周1次 | 长尾内容、UGC生态 |

使用 loop skill 或 cron 工具实现定时采集：
```
/loop 30m 继续采集话题「XXX」的最新数据并更新看板
```

## 采集工具详解

### kimi-webbridge 操作模式

kimi-webbridge 通过本地守护进程（`http://127.0.0.1:10086`）控制用户真实浏览器，复用登录态。

**关键命令**：
| 命令 | 用途 | 注意事项 |
|------|------|----------|
| `navigate` | 打开新页面 | `newTab:true` 开新标签页 |
| `snapshot` | 获取页面无障碍树 | 返回 `@e` 引用，用于 click/fill |
| `evaluate` | 执行 JS 代码 | 用 IIFE 包裹，结果用 `JSON.stringify` |
| `screenshot` | 截图 | 返回文件路径，用 Read 工具查看 |
| `find_tab` | 切回之前打开的标签 | 传入完整 URL |

**小红书抓取要点**：
- 搜索结果页用 `evaluate` + `document.querySelectorAll("section.note-item")` 提取
- 笔记内容通过 `item.innerText` 获取，包含标题、作者、时间、赞数
- 进入笔记详情页用 `click` 或 `evaluate` 点击卡片
- 评论区用 `document.querySelectorAll("[class*=comment]")` 提取

**抖音抓取要点**：
- 搜索结果用 `snapshot` 获取无障碍树效果更好
- 视频标题、互动数据在 `listitem` 节点中
- 热门标签反映用户关注焦点

**注意事项**：
- 登录态影响数据完整性，优先使用已登录浏览器
- 高频采集间隔不低于 5 秒，单次不超过 50 个页面
- 注意识别算法干预痕迹（置顶官方内容、过滤负面评论）

### 其他工具

| 工具 | 适用场景 |
|------|----------|
| websearch | 通用网络搜索（首选，若不可用则 webfetch 百度） |
| webfetch | 抓取特定 URL 全文、百度搜索结果页 |

## 数据目录结构

```
~/.local/share/data-research/
└── {topic-id}/
    ├── meta.json      # 话题元数据 + 关键词矩阵
    ├── entries.json    # 所有数据条目（含情绪分类）
    ├── evolution.json  # 演变节点记录（含KMI快照）
    └── signals.json    # 信号与洞察（含风险评估）
```

## 报告输出标准

每次调研输出标准化报告，包含以下章节：

| 章节 | 内容 | 必备程度 |
|------|------|----------|
| 核心发现 | 当前态势/评论区概况/信号概况/监控关键词，数据驱动自动生成 | 必备 |
| 发现了什么 | 从数据中发现的具体问题，每个发现必须有证据支撑 | 必备 |
| 意味着什么 | 对发现的解读，分析原因和影响 | 必备 |
| 建议怎么做 | 基于发现的具体建议，按优先级排列 | 必备 |
| 各平台评论区总结 | 每个数据源的评论区总结（高频词/情绪分布/典型评论/核心关注点） | 必备 |
| 信号与建议 | 每个信号带具体建议，按严重度排列，可直接参考执行 | 必备 |
| 竞品对比分析 | 主要对比对象、对比维度、用户倾向判断、关键发现（如有） | 按需 |
| 叙事主导权分析 | 品牌方/KOL/用户三方叙事占比、主导叙事、风险点（如有） | 按需 |
| 舆情风险矩阵 | 风险评估、重点风险详解、应对预案 | 必备 |
| 热点发酵预测 | 预测结论、触发条件、跟踪指标 | 推荐 |
| 数据附录 | 原始数据、Fact Check、采集时间戳 | 必备 |

### 各平台评论区总结模板

每个数据源（小红书/抖音等）都必须输出以下格式的评论区总结：

```markdown
### [平台名] 评论区总结

**采样范围**：头部内容 Top 10，共采样 N 条评论

**高频关键词 TOP 10**：
1. 关键词A（出现X次）
2. 关键词B（出现X次）
...

**情绪分布**：
- 正面：XX%（代表表述："xxx"）
- 负面：XX%（代表表述："xxx"）
- 中性：XX%
- 混合：XX%

**价格预期**：用户普遍预期 XX-XX 万元

**核心关注点排序**：
1. 空间/座椅布局
2. 价格/性价比
3. 续航/增程技术
4. 外观/内饰
5. 智能驾驶

**代表性评论**：
- 正面："xxx"
- 负面："xxx"
```

## 注意事项

- 所有中文内容保持原样，不做翻译
- 尊重数据源的访问频率限制，避免被封
- **每个数据源都必须采集评论区数据并做总结性归纳**，不可省略
- 评论区采样至少覆盖10个头部内容，每个采样前50-100条评论
- 情绪分类采用关键词匹配+人工判断结合，注意区分"真实负面"和"玩梗调侃"
- 演变分析要有证据支撑，不做无根据的推断
- 看板是文字为主体的，图表只是辅助理解趋势
- 数据持久化在 JSON 中，看板重启不丢失数据
- timestamp 必须使用采集时的真实时间，不可使用占位时间
- 每条数据必须标注 `collection_method`（kimi-webbridge / web_search / webfetch）
- 看板中所有标签使用中文：来源(小红书/抖音/网页搜索/社交媒体/评论区)、情感(正面/负面/中性/混合)、阶段(萌芽期/增长期/爆发期/衰退期/稳定期)、信号(严重/高风险/中等/低)
- 看板中无数据的模块自动隐藏，不显示空白区域

---

## 深度报告生成（完整版 · 数据驱动）

每次采集后可生成一份**可直接拿去汇报的完整版深度报告**，报告内容根据该话题实际数据动态定制——有数据的章节渲染、无数据的章节自动隐藏，绝不硬编码话题特定内容。

### 报告结构（14 章节，动态适配）

| 章节 | 内容 | 数据来源 | 无数据时 |
|------|------|----------|----------|
| 1. 核心结论速览 | 情绪基调结论 / 当前阶段 / 严重信号 / 操纵迹象 / 竞品焦点 | 情绪分布 + 信号 + 演变 | 始终渲染（结论自动推导） |
| 2. 调研数据基础 | 情绪分布图 / 平台分布图 / 高频词 TOP15 / KMI 指标表 | entries 统计 | 始终渲染 |
| 3. 正面情绪深度分析 | 正面占比 + 按互动量排序的代表内容引用 | 正面条目 | 隐藏 |
| 4. 负面情绪深度分析 | 负面占比 + 负面时间集中度 + 代表内容 | 负面条目 | 隐藏 |
| 5. 情绪来源追溯 | 按账号聚合：角色 / 立场 / 互动量 | author + content_type | 隐藏 |
| 6. 操纵痕迹判断 | 抹黑/AI投毒信号 + 负面时间集中度分析（自然发酵 vs 被推动） | 风险类信号 + 负面时间分布 | 始终渲染 |
| 7. KOL 画像与内容生态 | 已发声 KOL/媒体立场表 | content_type=KOL 条目 | 隐藏 |
| 8. 竞品对比分析 | 高频对比对象提及频次 | 内容关键词检测 | 隐藏 |
| 9. 舆情风险矩阵 | 信号按严重度分级 + 证据 + 建议 | signals | 隐藏 |
| 10. 预测研判 | 阶段推演 + 风险演变方向 | 最后演变阶段 + KMI | 始终渲染 |
| 11. 行动建议 | 按 P0/P1/P2 聚合建议 | signals.recommended_action | 隐藏 |
| 12. 话题演变时间线 | 各阶段 + 情感流向 + 关键词 | evolution | 隐藏 |
| 13. 各平台评论区洞察 | 评论区总结全文 | comment 条目 | 隐藏 |
| 14. 数据条目明细 | 最近 50 条原始数据 | entries | 始终渲染 |

### 使用方式

```bash
# CLI 生成（输出到 ~/.local/share/data-research/<topic-id>/report.html）
python3 scripts/report_generator.py "<topic_id>" --html

# 网页入口（看板顶栏「📊 报告」按钮）
http://127.0.0.1:8765/topic/<topic_id>/report

# API 手动重新生成
GET http://127.0.0.1:8765/api/report/<topic_id>
```

### 采集后自动更新

`auto_collect.py` 的 `run_collection` 在每次采集完成后自动调用 `generate_html_report` 重新生成报告（返回结果含 `report` 字段）。因此只需定时触发采集，报告会随之自动更新——无需手动重新生成。

### 完整版与精简版的演进说明

- **精简版**（早期）：固定 5 章节（数据概览 / 信号 / 时间线 / 评论区 / 条目），结构固定
- **完整版**（当前，推荐）：14 章节数据驱动深度报告，`analyze_topic()` 分析引擎从原始数据推导全部结论（情绪基调、负面集中度、竞品检测、操纵判断、KMI 指标、来源追溯），报告内容随话题和数据量变化，可直接用于汇报

### 报告生成器架构（report_generator.py）

```
analyze_topic(topic_id)  # 分析引擎：从 entries/signals/evolution 推导全部分析结果
  ├── 情绪/平台/方法/内容类型分布统计
  ├── KMI 指标（正面占比/负面占比/价格焦虑指数）
  ├── 来源追溯（author + content_type 聚合）
  ├── 竞品检测（通用品牌词表按内容频次）
  ├── 操纵证据（风险类信号 + 负面时间集中度 + 来源多样性）
  └── 叙事结构（content_type 占比）
_sec1.._sec14(a)          # 14 个章节渲染函数（各自判断数据存在性）
generate_html_report()    # 组装：head + 非空章节 + footer（含公众号二维码）
```

### 报告整合网站（2026-08-03 新增）

| 能力 | 实现 |
|------|------|
| 报告单独入口 | `/topic/<topic_id>/report` 路由 + 看板顶栏「📊 报告」按钮 + 首页卡片「查看深度报告」链接 |
| 采集后自动更新 | `auto_collect.run_collection()` 末尾调用 `generate_html_report` |
| 手动重生成 API | `GET/POST /api/report/<topic_id>` |
| 二维码 | `assets/qrcode.png`（原 BMP）→ `assets/qr_small.png`（压缩）→ `assets/qr_base64.txt`（base64 缓存），页脚固定「莫说闲话」公众号 |

### 发布到 GitHub

```bash
cd myou-data-research
git add -A
git commit -m "feat: data-driven deep report generator (14 sections) + dashboard report route + auto-update after collection"
git push origin main
```

### 报告生成注意事项

- 报告必须**数据驱动**，禁止硬编码任何话题特定内容（看板与报告同理）
- 所有标签用中文：来源/情感/阶段/信号
- 空章节自动隐藏，不显示空白区域
- 报告是文字为主体，图表只做辅助
- 页脚二维码固定「莫说闲话」公众号，生成报告时勿写其他名称
