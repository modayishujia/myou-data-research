# Changelog

## 2.2.0 — 2026-09-10

### 看板动态化与富文本

- 看板改为**按调研任务动态生成**：模块随场景与本轮数据组装，禁止套用固定成品页
- data.js 扩展长文案：态势 body、分析逻辑、发现 implication、意味着什么、信号 action、预测（结论/触发/跟踪/置信）、平台文字摘要、下轮计划
- 骨架 `dashboard_template.html` 仅作生成参考；交付为话题专属 `index.html`
- 生成原则：文字解读优先，数字必须带一句「意味着什么」

## 2.1.0 — 2026-09-10

### 本地实时看板

- 首轮完整报告后**必须询问**是否开启本地看板；监测方案增加 `dashboard` 字段
- 生成 `dashboard/index.html` + `data.js`（自包含、无 CDN，`file://` 可开）
- 每轮写完 research.md 后按契约重写 data.js；看板不替代报告，也不是采集触发器
- 新增 `references/dashboard.md`（协议）与 `references/dashboard_template.html`（视图模板）
- front matter 增加 `dashboard: true|false`

## 2.0.0 — 2026-09-10

### 结构

- **主文件瘦身**：`SKILL.md` 只保留原则、场景路由、Phase 1–5、存储与报告入口；模板/KMI/平台细节下沉 `references/`
- **references 拆分**（单一权威来源，避免与主文件双份维护）：
  - `templates.md` — research.md 完整骨架、数据行格式、去重规则、报告/预警/预案、增量读写与归档
  - `metrics.md` — 分层抽样、KMI、情绪/KOL、风险/预测/演变、中文标签
  - `data_sources.md` — 渠道优先级、动态选源、降级链、平台要点
  - `scenarios.md` — 5 场景链路、关键词占位、频率档
  - `examples.md` — 行业示例（可替换）

### 可执行性

- 评论区从「Top10×50–100 条」改为**分层抽样**（每源 Top 5–10 × 每内容 15–30 条，单轮约 80–200 条），并强制 `n=` 披露
- 补全 **research.md 模板**与去重键（`source+content_id` → url → 正文哈希）
- 增加**上下文预算**：每轮只读摘要+近 2–3 个演变节点；超 200KB / 40 轮归档切片
- 观测频率由监测方案 `frequency` + 场景默认档驱动，不再写死「发布前 30 天」为唯一节奏
- 预警仅结构化文本输出，**明确 skill 不发 Webhook/IM**

### 一致性

- 场景命名统一：产品**发布**（取消「产品舆情」混用）；章节序号 = 匹配优先级（1 金融 → 5 舆情）
- 样本量统一写作 `n=`；预警 JSON 去掉 skill 无法执行的 `deliver_to`
- 明确「分析信号」与「对外预警」是两套枚举，并给出映射
- Phase 5 频率表只保留在 `scenarios.md`，主文件改为指针
- 存储章节列表去掉已并入演变节点的独立「快照」
- keywords 落盘建议带 L1–L5 层级前缀
- 关键词「至少 10 组」改为「通常 5–15 组」
- 方法论内汽车示例改为占位符；`examples.md` 改为虚构品牌已填示例
- description 精简；英文版删除与「禁止机械套用 8 维度」矛盾的表述
- 采集能力描述泛化为「页面控制类浏览器工具」，不再绑定单一产品名
- README、agents 元数据与 2.0 结构对齐

## 1.2.0

- 场景预设扩充至 5 类（含金融单品追踪）
- 数据源动态匹配与降级链
- 分析改为「每轮盘点后生成逻辑」，8 维度降为候选池
- 样本量透明、基准自校准等原则

## 1.0.0

- 初始发布：全网采集 + 增量 markdown 沉淀 + 报告与预警流程
